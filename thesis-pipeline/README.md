# Thesis Pipeline (v1)

Standardized flow:
1. Put raw files in `input/`
2. Fill `meta/brief.yaml`
3. Run `scripts/ingest.sh`
4. Run `scripts/draft.sh`
5. Run `scripts/build.sh`

Outputs in `output/`.
