## 2026-03-15 18:06 CST - Durable memory flush

- User wants a real-time visual console to see exactly what the agent is doing, not just logs.
- Delivered and iterated a prototype at `prototypes/agent-visibility` with live SSE event stream from OpenClaw session JSONL.
- User preference evolved strongly toward third-person pixel-game style visualization (Minecraft/Stardew-like), focused on a single agent avatar rather than multi-agent fantasy.
- Added command center and control panel; user explicitly requested command visibility with clear status (what command, executing or not, progress).
- Upgraded UI to Vue 3 SPA-style tabs (dashboard / commands / skills / logs) to improve interaction and app-like experience.
- Added Skills explorer in UI: list available skills, open skill detail, inspect source files.
- Added login gate for Studio (auth required before entering UI/API) and command bridge from Studio to real OpenClaw session execution.
- Deployment status: service reachable at `claw.xrpcoder.xyz:8787`; HTTPS-on-8787 issue diagnosed as protocol mismatch (HTTP service on that port).
- User validated end-to-end command bridge by sending studio-originated test messages and receiving replies in chat.

## 2026-03-15 19:42 CST - Durable memory flush

- User repeatedly emphasized that product function design matters more than visual skin tweaks; UI should serve operational clarity.
- Stabilization work added metadata-noise filtering for image-only/transport-wrapper messages to avoid polluting command views and logs.
- Added frontend resilience for log rendering (UUID fallback, per-item recent parse isolation, log truncation) to prevent blank/frozen Studio.
- Current notable commits in this cycle: `1ad2b2a`, `cfabd0d`, `efb264e`, `c4bf36a`, `7ef419b`.
- User asked whether urgent insertion is truly usable; current behavior is real dispatch with urgent flag but not strict preemptive scheduler semantics yet.
- User requested temporary rollback from blue theme back to warm yellow for screenshot retention; yellow variant restored.
- User identified risk of iterative patching drift ("拆东墙补西墙") and requested a clean reset approach.
- Created decision document `prototypes/agent-visibility/VERSION_AUDIT.md` to inventory all historical versions/features and enable keep/remove decisions before redesign.
- Agreed working mode: first freeze scope (keep/remove + chosen visual direction), then produce PRD vNext, then rebuild in one coherent pass.
